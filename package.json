{
  "name": "imessage-ai-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "NodeGUI desktop shell for the iMessage AI agent",
  "main": "dist/main.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@nodegui/nodegui": "^0.56.0",
    "node-fetch": "^3.3.2",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
